{
  "name": "kaoqin",
  "version": "1.0.0",
  "description": "狂苗考勤",
  "main": "main.js",
  "scripts": {
    "start": "node app.js",
    "electron": "electron .",
    "pack": "electron-packager . kaoqin --asar.unpack=config.json --electron-version=1.6.11 --out=out --overwrite",
    "pack-32": "electron-packager . kaoqin --asar.unpack=config.json --electron-version=1.6.11 --out=out --arch=ia32 --overwrite",
    "pack-all": "electron-packager . kaoqin --asar.unpack=config.json --electron-version=1.6.11 --out=out --arch=all --overwrite"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4",
    "moment": "^2.18.1",
    "xlsx": "^0.8.8"
  },
  "devDependencies": {
    "electron": "^1.6.11",
    "electron-packager": "^8.7.2"
  }
}
